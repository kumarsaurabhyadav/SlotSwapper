{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node src/index.js",
    "dev": "nodemon --watch src --exec node src/index.js",
    "build": "echo \"No build step required for Node.js\"",
    "migrate": "node -e \"require('dotenv').config(); const {exec} = require('child_process'); exec('cat schema.sql | psql $DATABASE_URL', (err) => {if(err) console.error(err); else console.log('Schema applied'); process.exit(err?1:0);});\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.16.3",
    "socket.io": "^4.8.1",
    "uuid": "^13.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
